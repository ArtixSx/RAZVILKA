// Intentional UI migration: the old home composition is replaced, not the safety contract.
// Test extracted current production functions (not saved fixture implementations).
// Includes first-run, stale data, applied/desired separation, real zero metrics,
// auth fencing, navigation, inspector updates and job admission.
import './test-interface-unified.cjs';
