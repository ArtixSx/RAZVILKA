# Проверка EXT5 — 19–20 сентября 2026

Обновлено 20 сентября. **Кандидат 0.18.5 c7d92fd прошёл полный CI, применение,
восстановление после перезапуска приложения и очистку на ARM64-роутере.
Точные GitHub-файлы ещё не проверены; версия не опубликована.**
Исторические результаты разделены по локальной ARM64-сборке `c7ea788` и точным
GitHub-файлам 0.18.4 `5fceff8`. Для последних подтверждены CI, происхождение,
SHA256, установка и A/B; последующая приёмка перезапуска не прошла.
Финальная очистка завершена, исходная сеть и Safe Mode подтверждены.
Неопубликованные черновики 0.18.2/0.18.3/0.18.4
удалены после выявленных ошибок; копия 0.18.4 сохранена,
их Git-теги сохранены без переписывания.

Входная ревизия `3ab717ed9beb6ecb33117a182392466986132482`, база UPDATE4
`98b37263aa6446679c88f5e222f94e39e2e27eb6`. Git bundle содержит полную историю
и прежние исправления rc.5/rc.6. FULL SHA-256:
`d4e54ad9fd9f9b50c1838a19cba39bcc271d592811f9784f0806ff1249c530ee`.

## Исправления

1. Мастер повторно выбирал все сервисы при обновлении списка. Теперь сохраняет
   прежний выбор, оставляет новые пункты выключенными и отзывает старое согласие.
2. Занятое фоновое чтение блокировало актуализацию после сохранения. Новое чтение
   ждёт старого; ошибка чтения не выдаётся за ошибку записи.
3. Новый файл мог обрабатываться с прежним содержимым. Ввод очищается, действия
   блокируются до чтения, запоздалый ответ не заменяет новый ввод.
4. После входа оставалось сообщение о завершённой сессии в настройках NFQWS2.
   Оно очищается без дополнительных запросов или разрешений.
5. Предпросмотр Mihomo показывал технический JSON. Теперь выводит понятные
   параметры узла и явно сообщает, что подключение не проверялось и не включено.
6. Нормализация Mihomo теряла обязательные параметры. Непереносимые TLS-пины,
   ключи, detour и настройки транспорта теперь отклоняются. Проверки охватывают
   JSON, YAML, base64 и записи, скрываемые дедупликацией.
7. Уточнена проверка адресов серверов; HEV учитывает отмену до генерации файла.
8. Подпись пакета ломалась при экранировании символов JSON. Подписываются точные
   итоговые байты; экспорт учитывает размер, часы и целостность ключа.
9. Выбранная партия подключений удерживала общую очередь до завершения. Теперь
   уступает восстановлению после очистки каждого узла и повторно проверяет сеть.
10. Ошибка очистки скрывалась за отменой/сменой сети. Она имеет приоритет,
    сохраняется в результате и останавливает дальнейшие проверки.
11. При пустом журнале начальное восстановление не имело приоритета.
    Отсутствующие записи теперь ожидают первого прохода расписания.
12. Пакет Sing-box оставлял включённым штатный примерный сервер. Менеджер
    проверяет владельца init-скрипта, атомарно отключает штатный автозапуск
    после установки/обновления, включая частичный отказ opkg, и проверяет
    результат до выдачи успешного receipt. Чужие скрипты и процессы не меняются.
13. В аппаратной проверке Discord обнаружено ложное `response-body-invalid`:
    сервер игнорировал Range, а HTML превышал 32 КиБ. В 0.18.3 исправлено
    ограниченное чтение образца ответа. Проверка JSON и признаки блокировки
    сохраняются; произвольный ответ не выдаётся за сервисный PASS.
14. Discord возвращал пять IPv4 при пределе проверки четыре. Предел поднят
    до восьми; все адреса проверяются, общий предел времени сохранён.
15. Хранилище отклоняло отрицательный этап `service_ip` и некоторые русские
    сообщения. Исправлен контракт этапов и предел символов UTF-8; отмена
    и таймаут возвращают структурированный отчёт без успешного подтверждения.

## Программные проверки

Windows, Go 1.26.5: `go mod verify`, полный `go test -count=1 ./...`, `go vet ./...`.
Прошли 34 JavaScript-набора, проверка CSS и синтаксиса. Добавлены сценарии
отмены/очистки, смены сети, очередей, подписи/размера/повторного импорта и UI-сессии.
Для браузера использован настоящий изолированный Windows HTTP-сервер: тестовая
учётная запись, loopback, Safe Mode, без применения маршрутов. Пройдены **87
реальных HTTP-проверок** и проверки интерфейса на настольном и мобильном экранах.
Проверены вход, мастер и диалоги; предпросмотр Mihomo показывает параметры
подключения без технического JSON и без утверждения о работающем маршруте.

Исходные тесты HEV использовали непереносимый Linux-путь; исправлен тестовый
файл. POSIX-проверки доступа к ключам на Windows подтверждают отказ, не ослабляют
защиту. Первоначальный отчёт архива не считается результатом проверки новой ревизии.

Ревизия [`a5df27d`](https://github.com/ArtixSx/RAZVILKA/commit/a5df27d) прошла
полный [Linux CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35462661791):
Go-тесты, race, vet, все 34 набора JavaScript, сборки ARM64/MIPS/MIPSel/amd64
и изолированный прогон установки. Это программная проверка указанной ревизии,
а не подтверждение реального трафика на каждой архитектуре.

## Чистая установка на Netcraze ARM64

19 сентября проверенная ARM64-сборка `a5df27d` установлена на чистый Entware.
Вход `admin` подтверждён. Панель находится в Safe Mode, списки выбранных
и применённых сервисов пусты. Старые маршруты и пользовательская конфигурация
с очищенной флешки не восстанавливались.

Маршруты основной таблицы и `ip rules` успешно прочитаны до и после установки
(код завершения 0); полное сравнение совпало. Перезагрузка роутера не выполнялась.
Проверены браузерная панель роутера и сводка Mihomo.

**Firewall этим сравнением не подтверждён.** В чистой Entware не было
`iptables-save`/`ip6tables-save`: до и после установки получен код 127.
Равенство двух ошибок первоначально было ошибочно принято проверочным помощником
за совпадение снимков. Этот ранний результат исключён из успешных. Позже утилиты
установлены и проверка выполнена заново, как описано ниже.

## Проверка точного архива 0.18.2 и установка Sing-box

Ревизия `7e70e3ed9e781c87345c76231fa514d5dcb84599` прошла
[Release CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35463851951).
Проверены GitHub attestation архива и все контрольные суммы SHA256.

- SHA256 архива: `41fa361f7ea6fdd9ed5e5e58cf99c3d085ba33113521c8a9251258453d47a13f`.
- SHA256 установленного ARM64-бинарника: `eb5ebcddfeb00ac0a923fd871c81233a68fd7b7270d41d1a1ab7ca80f9887d69`.

Учётные данные и настройки сохранены; в хранилище остались пять узлов.
Safe Mode включён. Через API установлен Sing-box `1.13.3-2`; бинарник содержит
gVisor, штатный init имеет `ENABLED=no`, примерный сервер не запущен.

После установки системных утилит main routes, `ip rules`, `iptables-save`
и `ip6tables-save` действительно завершались кодом 0. Снимки до/после обновления
панели и установки Sing-box совпали после исключения изменяемых счётчиков.
Эта проверка не является перезагрузкой роутера или применением маршрута сервиса.

## Реальные VLESS-проверки: обнаруженный отказ

Приватный `router-node-checks.private.json` содержит результаты пяти разных
публичных узлов. Сводка проверена по этапам, адреса и ключи здесь не публикуются.

| Количество | Протокол и выход | Сервисный результат |
| --- | --- | --- |
| 3 | Оба этапа пройдены | Discord: `response-body-invalid`, HTML больше 32 КиБ при проигнорированном Range |
| 1 | Оба этапа пройдены | `request-failed` |
| 1 | Оба этапа не пройдены | `node-egress-failed`, сервис не подтверждён |

Итого **4/5 подтвердили протокол и выход, 0/5 получили сервисный PASS**.
Все результаты имеют `available=false` и `working_routes_changed=false`.
На этой ревизии (0.18.2) маршруты ещё не применялись; свежий трафик LAN-клиента
через применённый обход не был подтверждён. HTTP 200 от API означает полученный отчёт, а не
успешное подключение.

Отдельно загрузка источника CIDR Telegram завершилась таймаутом
`core.telegram.org`; Raw GitHub вернул HTTP 200. Это не ошибка TLS и не результат
проверки Discord через VLESS.

## Точный архив 0.18.3 и проверка CDN

Ревизия `a0fdbf5b8a56a60cacacfb809bbdffe62183a57f` прошла
[Release CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35465006674)
и 87 проверок реального изолированного HTTP-сервера. Проверены все SHA256
и GitHub attestation. Точный ARM64-бинарник установлен, настройки и пять узлов сохранены.

- Архив: `d47b0070923cbc22645558997ac23ce857f7faa5d58345f663d577f6b2975604`.
- ARM64: `2b60775386b7d409fc2d14fc46b131d4f4217de100718c1e199753dbc83c5934`.

Повторная проверка выявила два общих HTTP 503 после перехода к IP-пути.
Контракт хранилища не принимал этап `service_ip`; текущий DNS-ответ Discord
содержал пять IPv4 при пределе четыре. Эти проверки не считаются успешными.
Панель 0.18.3 проверена в браузере: вход и редактор NFQWS2 загружаются.

## Проверка локальной ARM64-сборки c7ea788

Ревизия `c7ea788` прошла [полный CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35466448079).
Из точного исходного кода локально собран ARM64-бинарник и установлен штатным
установщиком. Настройки, пять узлов и вход `admin` сохранены. Следующие результаты
относятся только к локальной сборке; отдельная проверка GitHub-файлов описана ниже.

Предел набора CDN расширен с четырёх до восьми IPv4; каждый адрес должен пройти
проверку в пределах общего бюджета 45 секунд. Отказы `service_ip` сохраняются;
таймаут и отмена возвращают структурированный отрицательный отчёт без записи
(`recorded:false`). Сообщения ограничены 240 символами
без повреждения UTF-8. Из пяти узлов два получили сервисный PASS Discord
по имени и всем пяти адресам CDN.

| Сценарий | Свежий клиентский трафик | Собственные счётчики |
| --- | --- | --- |
| Маршрут A для одного ПК | 3 HTTPS 200 | +7 пакетов; TUN RX 578171 / TX 16904 байт |
| Ручной переход на B | 3 HTTPS 200 | +6 пакетов; RX 583684 / TX 19444 байт |
| Перезапуск приложения | 3 HTTPS 200 | +6 пакетов; RX 581384 / TX 18248 байт |

Синтетический запрос маршрута вне области прошёл; физический второй клиент
не проверялся. Переключение A/B было ручным, без инъекции аварии основного узла.
Один сброс наблюдателя отозвал прежнее подтверждение B. Восстановление A завершилось,
повторная проверка B разрешила переход. Позже сброс повторился на итоговом
GitHub-бинарнике; считать его единичным нельзя. Причина пока неизвестна.

Остановка собственных маршрутов и Safe Mode восстановили исходные main routes,
policy rules и IPv4/IPv6 firewall, исключая счётчики. Управляемых процессов
Sing-box, TUN и слушателей не осталось. Идентичность загрузки роутера сохранилась:
проверялся перезапуск приложения, а не reboot. Приватные отчёты клиентских
проверок сохранены локально; адрес ПК, топология и ключи не публикуются.

## Точные итоговые файлы GitHub Release 0.18.4

Ревизия `5fceff8cdf3f9c2ad0a9d795169e3263cfe1352c` прошла
[Release CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35467156265)
и оба запуска CI ([35467156225](https://github.com/ArtixSx/RAZVILKA/actions/runs/35467156225),
[35467156248](https://github.com/ArtixSx/RAZVILKA/actions/runs/35467156248)).
Проверены GitHub attestation архива и все SHA256; штатным обновлением установлен
точный ARM64-бинарник, сохранены учётные данные, настройки и пять узлов.

- SHA256 архива: `84d0d4089a7748936e959812f61079bf6368b4f5bf6d4d002a4a13136f4550e7`.
- SHA256 ARM64: `92b72e9719afd9f58757321b47de0f51f92ac99b57ac9f86a617fa986c725bab`.

Всего проверено шесть публичных узлов (пять прежних и один добавленный);
два получили сервисный PASS. Первая попытка применения A откатилась при
сбросе наблюдателя: поколение изменилось с 1 на 3. Исходная сеть восстановлена
полностью, проверки не отключались. Повторное применение A и переход B прошли.

| Сценарий точного GitHub-бинарника | Клиентский результат | Собственные счётчики |
| --- | --- | --- |
| Повторное применение A для одного ПК | 3 HTTPS 200 | +6 пакетов; TUN RX 577627 / TX 16584 байт |
| Ручное переключение B | 3 HTTPS 200 | +6 пакетов; TUN RX 580433 / TX 17636 байт |

Синтетический запрос источника вне области обходил TUN; физический второй
клиент не проверялся. Адреса ПК, узлов и топология не публикуются.
Приватные отчёты A/B и отката сохранены локально.

Диагностическое чтение за 75 секунд собрало 150 образцов без ошибок. Это не
объясняет прерывистый сброс наблюдателя, отмеченный на локальной и итоговой
сборках. Правильный откат подтверждён; расследование причины остаётся открытым.

## Перезапуск 0.18.4: отказ приёмки и завершённая очистка

После успешных A/B точного бинарника приёмка восстановления после перезапуска
не прошла. Две автоматические перепроверки завершились
`INCONCLUSIVE / node-egress-control-unavailable` примерно за 2,2 секунды;
поколение сети оставалось стабильным. Прямой curl к двум контрольным HTTPS-целям
вернул 200, что само по себе не объясняет результат проверяющего модуля.

Затем подтверждён дефект reconciler: восстановление наследовало старый backoff
узла B — шесть попыток, следующий запуск примерно через 16 минут. Этот дефект
планировщика не является доказанной причиной исторического сброса наблюдателя.
Неопубликованный черновик 0.18.4 сохранён и удалён, тег оставлен; её успешные A/B
сохраняются как отдельные исторические результаты, а restart не объявляется успешным.

Маршруты остановлены, Safe Mode включён. В приватном отчёте финальной очистки
все девять проверок имеют `true`: исходные main routes, policy rules и IPv4/IPv6
firewall восстановлены (кроме счётчиков); загрузка роутера та же; собственных
Sing-box, TUN, проверочных/штатных слушателей и применённых маршрутов нет.

## Кандидат 0.18.5: CI, восстановление и завершённая очистка

Исправлены двойной backoff, сброс и пробуждение node-recovery при изменении
разрешённого состояния восстановления. Добавлены диагностика наблюдателя
и ограниченный повтор прерванного netlink dump. Причина прежних прерывистых
сбросов не поймана; повтор dump не объявляется доказанным исправлением этой причины.

20 сентября для рабочего дерева прошли полный пакет app (79,4 секунды), целевые
тесты восстановления (16,3 секунды) и vet. Модульные проверки наблюдателя
и vet прошли на Windows. Linux ARM64 systemprobe на настоящем роутере, включая
чтение сетевого состояния без изменений: 43 PASS, два opt-in теста пропущены.
Затем ревизия `c7d92fda4266544151dcf37f9792c021ea704e05` прошла полный
[CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35470186140).

Локально собранный ARM64-кандидат этой ревизии установлен на роутер.
Свежая сервисная проверка A прошла; маршрут Discord применён только для одного ПК.
После перезапуска приложения node-recovery запустилась немедленно и завершилась
`recovered / committed` с первой попытки.

| Сценарий кандидата 0.18.5 | Клиентский результат | Собственные счётчики |
| --- | --- | --- |
| Применение A для одного ПК | 3 HTTPS 200 | +6 пакетов; TUN RX 576985 / TX 16324 байт |
| После восстановления при перезапуске приложения | 3 HTTPS 200 | +6 пакетов; TUN RX 577280 / TX 16584 байт |

Резервный узел B также получил свежий сервисный PASS. Это не проверка ручного
переключения A/B кандидата или автоматического перехода при намеренном отказе.

Очистка кандидата завершена: все девять проверок приватного отчёта имеют `true`.
Исходные main routes, policy rules, IPv4/IPv6 firewall совпали с baseline
(кроме счётчиков); загрузка роутера та же. Safe Mode включён, собственных
Sing-box, TUN и проверочных слушателей нет. Приватные отчёты сохранены локально;
адреса ПК, узлов и топология не публикуются.

Далее нужны сборка выпуска, проверка происхождения и SHA256 точных
GitHub-файлов 0.18.5, установка, A/B, восстановление после перезапуска и очистка.
Результаты кандидата не объявляются проверкой ещё не испытанных файлов выпуска.

После проверки кандидата визуальный осмотр выявил историческое сообщение
`recovered`, остававшееся после остановки маршрутов. Сообщение об успехе убрано;
актуальное состояние показывает общий переключатель. Предупреждения о повторной
проверке, устаревших данных и необходимости проверки сохранены. Проверки
node-route/browser прошли; эта последующая правка интерфейса будет проверена CI
и аппаратной приёмкой точных файлов выпуска отдельно от кандидата `c7d92fd`.

Mihomo/HEV, автоматический переход при аварии VLESS, WAN/reboot, IPv6/UDP, физический второй клиент
и длительная устойчивость этой проверкой не подтверждены. Онлайн-каталог
стратегий и ночная автоматическая установка не реализованы.

## История публикаций

По запросу владельца удалены семь старых prerelease-записей: `0.18.1-rc.1`,
`0.18.1-rc.2` и `0.18.2-rc.2`–`rc.6`. Git-теги, история кода и стабильные
выпуски сохранены. Старые проверки остаются историей своих ревизий.

Неопубликованные черновики 0.18.2, 0.18.3 и 0.18.4 удалены; их Git-теги сохраняются.
Копия черновика 0.18.4 сохранена после отказа приёмки перезапуска.
Для 0.18.5 подготовлено [краткое описание добавлений и исправлений](releases/0.18.5.md);
подробные результаты и ограничения остаются в этом отчёте.
