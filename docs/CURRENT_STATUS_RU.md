# Текущий статус RAZVILKA

## Исправления панели и первый этап автоматики 0.18.9

Исправлен запуск обновлений состояния после входа через сохранённую сессию:
общий переключатель больше не остаётся без данных из-за скрытой формы входа.
Он останавливает и возобновляет ранее применённые маршруты; панель остаётся
доступной. Перед возобновлением проверяются сохранённые подключения. В безопасном
режиме запуск требует разрешения в настройках. Кнопка «Лог» показывает причины
отказов и последние действия.

Аппаратная проверка также обнаружила слишком короткий двухсекундный лимит:
маршрут передавал клиентский трафик, но полная проверка его сетевых правил
обрывалась. Проверка собственного процесса и всех правил сохранена; её лимит
увеличен до восьми секунд отдельно от проверки свежести сети. Отмена запроса
учитывается, а таймаут и другие причины неподтверждённого состояния видны в логе.

В карточках обходов появились проверка версий, установленная и доступная версии,
установка и удаление. Ошибка обновления каталога больше не скрывает установленный
компонент. Отдельно учитываются работающий процесс, внешний владелец, зависимости
и устаревшие данные. USQUE и Xray используют Sing-box как сетевую зависимость;
его нельзя удалить или обновить под выбранным либо действующим маршрутом.

На роутере найден загрузчик Entware без HTTPS, мешавший обновлению каталогов
NFQWS2 и WARP. Установлен `wget-ssl`, обновление всех подключённых каталогов прошло.
Установщик теперь исправляет этот случай и повторно проверяет результат.
Из 35 источников сервисов недоступен был IP-список Telegram на `core.telegram.org`.
Он заменён на каталог Loyalsoldier в GitHub с явным указанием происхождения:
там объединены официальный список и данные ASN. Частичная загрузка доменов без
обязательного IP-списка не считается успешным импортом.

Автопилот теперь может перейти к следующему резерву после свежего подтверждённого
отказа кандидата в пределах установленного лимита. Неопределённый результат,
потеря наблюдения сети, ошибка очистки или применения останавливают раунд.
Полный сценарий «успешная проба B → отказ применения B → откат → применение C»
этим изменением **не закрыт**.

[Новый план из 15 этапов](plans/automation-2026-09-20/README.md) сверён с текущим
кодом. Его 104 сценария сохраняют статус запланированных; локальные проверки
отдельных исправлений не подменяют аппаратную приёмку всей матрицы.
Проверки версий и подготовка архива по расписанию существуют. Полностью
автоматическая установка обновлений приложения и компонентов остаётся отдельной
незавершённой работой; настройка расписания не выдаётся за её реализацию.

Точные файлы 0.18.9 собраны из `78be7ef62db23b736db34dd5dc777d5a91997638`.
Прошли полный локальный набор Go-проверок, проверки интерфейса, три запуска CI
и [сборка выпуска](https://github.com/ArtixSx/RAZVILKA/actions/runs/35478011260).
У всех десяти файлов проверены размеры, SHA256 и манифесты; у архива — также
происхождение из подписанного задания GitHub. Формат бинарников проверен для
ARM64, MIPS, MIPSel и тестового AMD64.

На Netcraze с ARM64 установлен именно этот выпуск. Настройки, учётная запись
и восемь подключений сохранены. На точном архиве прошли изолированные проверки
подготовки Entware, удаления, установки, повторного обновления, отката и отказов.
Все 35 источников каталога сервисов получили успешный предварительный просмотр;
Telegram загрузил 21 домен и 12 сетей. Обновление каталогов компонентов прошло.
Цикл установки и удаления `wgcf` проверен на кандидате того же исходного кода,
без создания аккаунта или туннеля.

Выключение и повторное включение выполнены кнопкой в браузере. После включения
переключатель показал «Проект включён»; после перезагрузки страницы сохранённая
сессия снова получила состояние. Три настоящих HTTPS-запроса к Discord прошли
с одного компьютера через собственные правила и TUN, что подтверждено счётчиками.
Маршрут ограничен этим компьютером. Запрос маршрута для другого исходного адреса
обошёл TUN; это не заменяет испытание на втором физическом клиенте.
Для этого сервиса включена проверка каждые пять минут. Первый запуск по таймеру
завершился успешно, сервер сохранил время следующей проверки. Это проверка
доступности, а не подтверждение полной автоматической установки и восстановления.

После выключения собственные процессы, TUN и правила убраны, основная таблица
маршрутов, policy rules и IPv6 firewall сохранились. В IPv4 firewall отдельно
отмечено изменение адреса и порта в постороннем правиле Cloud службы прошивки;
побайтовое совпадение всего firewall не заявляется. Семь временных каталогов
испытаний удалены, резервные копии сохранены.

**Открытая проблема:** наблюдатель сети иногда теряет непрерывность наблюдения
на этапе `event-drain`. Трафик может продолжать идти, но прежнее подтверждение
маршрута становится недействительным. Переключатель остаётся включённым,
показывает «Нужна проверка», а лог объясняет причину. Точная причина сброса
и устойчивое автоматическое восстановление после него ещё не подтверждены.
Это отдельная задача нового плана, не исправленная увеличением лимита проверки.

[Выпуск 0.18.9](https://github.com/ArtixSx/RAZVILKA/releases/tag/v0.18.9) ·
[Изменения](releases/0.18.9.md) · [Установка](INSTALL_RU.md).

## Обновление установки 0.18.6

Подготовлены установка в две команды, автоматическая проверка недостающих
утилит Entware и отдельные действия удаления и отката. Обновлена главная страница,
прежние инструкции кандидатов перенесены в архив. Пакеты и официальные архивы
Entware проверены для `aarch64-k3.10`, `mipssf-k3.4` и `mipselsf-k3.4`.

Локальные проверки bootstrap, удаления, архитектуры и контрольных сумм прошли:
включая отсутствие утилит, отказ загрузки, повреждённый архив и сохранение данных
при неудачной остановке или очистке. На ARM64 прошли изолированные проверки
настоящего бинарника: установка, повторное обновление, откат, конфликт порта,
отказ после неудачной остановки и удаление после обновления с сохранением данных.
Bootstrap и удаление отдельно проверены с BusyBox на роутере.

Точные файлы выпуска `743e14ffe1c4151d006283b8e68a99f14d6f9b75` прошли
[Release CI](https://github.com/ArtixSx/RAZVILKA/actions/runs/35472858831)
и оба CI. Проверены происхождение архива, исходный тег `v0.18.6`, размеры и
SHA256 всех десяти файлов выпуска. На точном ARM64-бинарнике повторно прошли
bootstrap, удаление и полный изолированный цикл установки, обновления и отката.
Рабочая панель обновлена до 0.18.6: настройки, учётная запись и восемь подключений
сохранены. Safe Mode включён, активных маршрутов приложения нет.

Обе опубликованные команды README выполнены на роутере: подготовка пакетов,
загрузка bootstrap с `main`, получение стабильного выпуска из GitHub, проверка
архива и штатное обновление. Получен тот же ARM64-бинарник; настройки и подключения
сохранены. Финальная сверка исходной сети и собственных ресурсов прошла все девять
проверок. Три временных каталога испытаний удалены, снимки для отката сохранены.

[Выпуск 0.18.6](https://github.com/ArtixSx/RAZVILKA/releases/tag/v0.18.6) ·
[Изменения](releases/0.18.6.md) · [Установка](INSTALL_RU.md).

Проверки клиентского трафика 0.18.5 ниже относятся именно к прежней версии;
в 0.18.6 менялись установочные скрипты и документация, эти испытания не повторялись.
MIPS/MIPSel остаются обязательными целями сборки; физических испытаний этих
двух архитектур пока нет. OpenWrt записан как отдельная будущая задача.

## Подтверждённый выпуск 0.18.5

Обновлено 20 сентября 2026 года. **Точные файлы 0.18.5 `39ab445` прошли CI,
проверку происхождения и SHA256, установку, применение, переключение,
восстановление после перезапуска приложения и финальную очистку на ARM64.**
Собственные маршруты остановлены, Safe Mode включён,
исходная сеть восстановлена. Результаты кандидата приведены отдельно.

[Выпуск 0.18.5](https://github.com/ArtixSx/RAZVILKA/releases/tag/v0.18.5) ·
[Изменения](releases/0.18.5.md) ·
[Подробный аудит](EXT5_REVIEW_2026-09-19_RU.md) ·
[Release CI 39ab445](https://github.com/ArtixSx/RAZVILKA/actions/runs/35470707377)

## Точные файлы выпуска 0.18.5

Ревизия `39ab4457fe6fc96437b31217b203a136c27a8a0d` прошла Release CI и оба
запуска CI. Проверена attestation архива с исходным тегом `v0.18.5` и этой
ревизией; все десять файлов сверены по SHA256, размерам и манифестам.
Штатное обновление установило точный ARM64-бинарник, сохранив учётные данные,
настройки и восемь узлов.

| Сценарий точного выпуска | Подтверждённый результат |
| --- | --- |
| Применение C для одного ПК | Свежий сервисный PASS; три HTTPS 200, собственные счётчики +6 пакетов, TUN RX 582103 / TX 18924 байт |
| Переключение C → A без остановки | Свежая проверка A прошла, переключение применено; три HTTPS 200, +7 пакетов, TUN RX 577448 / TX 16540 байт |
| Перезапуск приложения | Свежая проверка восстановления завершилась `recovered / committed` с первой попытки; три HTTPS 200, +6 пакетов, TUN RX 576486 / TX 16012 байт |
| Финальная очистка и Safe Mode | Все девять проверок прошли: исходные main routes, policy rules, IPv4/IPv6 firewall и загрузка роутера совпали; собственных Sing-box, TUN и проверочных слушателей нет |

Сравнение firewall исключает изменяемые счётчики. Роутер не перезагружался.
Удалены семь проверенных временных каталогов установки на накопителе;
резервные копии для отката сохранены. Занято около 194 МБ, свободно около 6,7 ГБ.
Автоматический переход на резерв при намеренном отказе этим испытанием не подтверждён.

## Подтверждено для кандидата 0.18.5

Локально собранный ARM64-кандидат
`c7d92fda4266544151dcf37f9792c021ea704e05` установлен на роутер.
Свежая сервисная проверка узла A прошла; маршрут Discord применён только
для одного ПК. После перезапуска приложения проверка восстановления запустилась
сразу и с первой попытки достигла состояния `recovered / committed`.

| Сценарий кандидата | Подтверждённый результат |
| --- | --- |
| Применение A для одного ПК | Три HTTPS 200; собственные счётчики +6 пакетов, TUN RX 576985 / TX 16324 байт |
| Восстановление после перезапуска приложения | Три HTTPS 200; +6 пакетов, RX 577280 / TX 16584 байт |
| Применение резервного узла B после свежего PASS | Сервисный IP-путь недоступен при проверке применения; выполнен откат, все девять проверок очистки прошли |
| Применение дополнительного узла C | Сервисный PASS, маршрут применён; три HTTPS 200, +6 пакетов, TUN RX 580632 / TX 18208 байт |
| Остановка после A/C и откат B | Все девять проверок очистки прошли в каждом случае: исходные маршруты, policy rules и IPv4/IPv6 firewall восстановлены, собственных процессов, TUN и слушателей нет |

Из двух дополнительно проверенных публичных узлов первый не прошёл проверку
выхода, второй (C) подтвердил сервис и клиентский трафик. Ранее полученный PASS
узла B не заменил проверку непосредственно при применении: при отказе внешнего
пути изменения откатились, проверки не ослаблялись. A и C применялись отдельно,
с остановкой между ними; непрерывное переключение A → C на кандидате не проверялось.

Перезагрузки роутера не было. Сравнение firewall исключает изменяемые счётчики.
Эти результаты относятся к кандидату; точные файлы выпуска проверяются отдельно.

## Исправления восстановления

Подтверждён дефект планировщика восстановления: после смены состояния оставалась
старая задержка узла B — шесть попыток и следующий запуск примерно через 16 минут.
Исправлены двойное увеличение задержки, её сброс и пробуждение при новом
разрешённом состоянии восстановления. Перезапуск кандидата подтвердил
немедленную перепроверку и успешное восстановление с первой попытки.

Добавлены диагностика наблюдателя и ограниченный повтор прерванного чтения
netlink. Это не установленная причина прежних прерывистых сбросов наблюдателя;
она остаётся предметом расследования. Успешная проверка кандидата не доказывает,
что историческая причина сбросов устранена.

Пакет app, целевые тесты восстановления и vet прошли. Проверки наблюдателя
прошли на Windows; Linux ARM64 systemprobe на настоящем роутере, включая
чтение сетевого состояния без изменений: 43 PASS, два opt-in теста пропущены.
Общий CI кандидата 0.18.5 прошёл; результаты аппаратной проверки приведены выше.

## Сохранённые исправления EXT5

- Сервисные проверки читают ограниченный образец большого HTML-ответа, даже
  если сервер игнорирует Range. Проверки JSON и признаков блокировки сохранены.
- Допускается до восьми IPv4 CDN вместо четырёх; каждый адрес должен пройти
  проверку в пределах общего бюджета 45 секунд.
- Отказы этапа `service_ip` сохраняются; таймаут и отмена возвращают через API
  структурированный отрицательный отчёт без записи (`recorded:false`).
  Диагностика ограничена 240 символами без повреждения UTF-8.
- Исправлены загрузка панели, редактор NFQWS2, сохранение мастера и импорт
  файлов, подписи стратегий, очередь проверок и штатный автозапуск Sing-box.

## История проверок 0.18.4 на Netcraze ARM64

Использован **точный ARM64-бинарник из GitHub Release**, ревизия
`5fceff8cdf3f9c2ad0a9d795169e3263cfe1352c`. Проверены attestation и контрольные
суммы. Штатное обновление сохранило настройки, пять узлов и вход `admin`;
затем для проверки добавлен ещё один публичный узел.

| Сценарий | Подтверждённый результат |
| --- | --- |
| Шесть публичных VLESS-узлов | Два получили сервисный PASS Discord: проверены доменное имя и все пять адресов CDN |
| Повторное применение A только для одного ПК | Три свежих HTTPS 200; собственные счётчики +6 пакетов, TUN RX 577627 / TX 16584 байт |
| Ручное переключение на B | Три HTTPS 200; +6 пакетов, RX 580433 / TX 17636 байт |
| Первая попытка A при сбросе наблюдателя | Применение отклонено; выполнен откат с полным восстановлением исходных правил сети |
| Последующий перезапуск приложения | Приёмка не пройдена: восстановление задержано старым backoff узла B |
| Финальная остановка и Safe Mode | Все девять проверок очистки прошли: исходные маршруты и firewall восстановлены, собственных процессов, TUN и слушателей нет |

Перезагрузки роутера не было. Синтетический запрос маршрута для адреса вне
выбранной области прошёл; физический второй клиент не испытывался.
Ручное A/B не является проверкой автоматического переключения при аварии.

Наблюдается **прерывистый сброс наблюдателя**: он встречался и на локальной,
и на итоговой сборке. В первой попытке A поколение сменилось с 1 на 3;
проверки не были ослаблены, транзакция откатилась, исходная сеть восстановлена.
Повторные A/B прошли. Чтение диагностики за 75 секунд (150 образцов) ошибок
не показало, но причину сброса не установило. Расследование остаётся открытым.

## Другие подтверждения

- `c7ea788`: локальная ARM64-сборка прошла A/B, перезапуск приложения и полную
  остановку собственных ресурсов. Эти исторические результаты не отменяют
  последующий отказ приёмки перезапуска точных GitHub-файлов 0.18.4.
- `a5df27d`: полный Linux CI, 87 проверок реального изолированного HTTP-сервера,
  настольный и мобильный браузер, чистая установка панели и вход.
- `7e70e3e` (неопубликованный тег 0.18.2): проверены attestation архива и SHA256,
  точный ARM64 установлен с сохранением настроек и пяти узлов.
- Sing-box `1.13.3-2` установлен через API, с gVisor и `ENABLED=no`;
  штатный примерный сервер не запущен.
- После установки системных утилит снимки firewall, маршрутов и policy rules
  прочитаны успешно. Ранние ошибки отсутствующих утилит с кодом 127 исключены
  из успешных результатов.

## Что остаётся

1. Автоматическое переключение с намеренным отказом основного узла, WAN reconnect,
   reboot, физический второй клиент и длительная работа.
2. Расследовать прерывистый сброс наблюдателя; проверить IPv6/UDP, MIPS/MIPSel
   и работу при ограниченной памяти.
3. Mihomo/HEV: нативный запуск и интеграция управляемых маршрутов. Сейчас доступны
   конфигураторы, а не готовые проверенные обходы.
4. Подписанные автоматические обновления с полным откатом, онлайн-пакеты стратегий
   и DNS для конкретного сервиса/устройства.

Загрузка CIDR Telegram отдельно остановилась по таймауту `core.telegram.org`;
Raw GitHub отвечал HTTP 200. Это не ошибка TLS и не опровержение проверки Discord.

## Публикации

Семь старых prerelease-записей удалены. Неопубликованные черновики 0.18.2, 0.18.3
и 0.18.4 также удалены; копия черновика 0.18.4 сохранена. Git-теги, история
исходников и прежние стабильные выпуски сохранены. 0.18.4 не публиковалась
из-за дефекта восстановления; её успешные A/B сохранены как история.
Точные файлы 0.18.5 прошли описанную выше приёмку. Результаты каждой сборки
относятся только к указанной ревизии.

[Releases](https://github.com/ArtixSx/RAZVILKA/releases) · [План](ROADMAP.md)
